with open('userinput2file.txt','w') as destination:
    while True:
        date = input("Text for file or EOF? "
                if data.lower() == 'eof'
                    break

                destination.write(data + '\n')
